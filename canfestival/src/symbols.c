/* symbols.c — Linux kernel module symbol exports (not used on embedded)
 * This file is intentionally empty for embedded platforms.
 * The RT-Thread Studio build system auto-discovers all .c files,
 * so we must keep this file but make it compilable.
 */
/* Intentionally empty — no Linux kernel module support on embedded */
